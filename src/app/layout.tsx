import './globals.css';
export const metadata={title:'Bodhi Rural | Livelihood & Agri',description:'Bodhi Rural farmer and agriculture digital platform'};
export default function RootLayout({children}:{children:React.ReactNode}){return <>{children}</>}
